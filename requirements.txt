# Only needed to rebuild data, and only by these scripts:
#   refresh_addresses.py    requests + shapely
#   refresh_precincts.py    requests + shapely
#   build_graph_chunks.py   shapely
#   build_precincts.py      shapely
#   geocode_places.py       requests, for the parcel pull only (and node,
#                           for the centreline pass)
# merge_foia_dropboxes.py uses geocode_places.py's helpers but never fetches,
# so it needs only node, for the same centreline pass. Every other script is
# standard library only, and the site itself needs nothing installed at all.
requests>=2.31
shapely>=2.0
