{
  "name": "votegr",
  "private": true,
  "description": "Tooling manifest. The site itself has no build step and no dependencies; this file exists so the browser suites have a browser to drive.",
  "license": "Unlicense",
  "devDependencies": {
    "playwright": "1.56.1"
  }
}
